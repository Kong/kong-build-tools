dist: xenial
sudo: false
language: minimal

before_install:
  - sudo make setup_tests
  - sudo iptables -P FORWARD ACCEPT
  - git clone git@github.com:Kong/kong.git ../kong

install:
  - make package-kong

script:
  - make test